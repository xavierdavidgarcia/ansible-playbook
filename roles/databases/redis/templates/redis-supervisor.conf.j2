[program:Redis]
command= redis-server /srv/conf/redis/redis.conf
numprocs=1                    ; number of processes copies to start (def 1)
autostart=true                ; start at supervisord start (default: true)
autorestart=true              ; retstart at unexpected quit (default: true)
startsecs=60                  ; number of secs prog must stay running (def. 1)
startretries=3                ; max # of serial start failures (default 3)
user=redis                   ; setuid to this UNIX account to run the program
stderr_logfile = /srv/logs/redis/redis-stderr.log
stdout_logfile = /srv/logs/redis/redis-stdout.log

